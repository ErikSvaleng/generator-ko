language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-classical-ko' ]; then cd .. && eval "mv $currentfolder generator-classical-ko" && cd generator-ko; fi

